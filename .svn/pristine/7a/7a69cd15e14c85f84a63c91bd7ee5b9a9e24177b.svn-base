extends ../layout
block body
	br
	div.row
		div.col-lg-4
			div.panel.panel-primary
				div.panel-heading Speicherverwaltung
				table.table.table-striped
					tr
						th Anzahl der Aufnahmen
						td #{picCount}
					tr
						th Verwendeter Speicherplatz
						td #{usedMemory} MB
					tr
						th Noch verfügbarer Speicherplatz
						td 50 MB
		div.col-lg-4
			div.panel.panel-primary
				div.panel-heading Bewegungserkennung
				table.table.table-striped
					tr
						th Ein-/Ausschalten
						td
							div.btn-group
								button(id="detectionOn", type="button", class=(detectionState)?'active':'', onclick="toggleDetection()").btn.btn-success Ein
								button(id="detectionOff",type="button", class=(!detectionState)?'active':'', onclick="toggleDetection()").btn.btn-danger Aus
					tr
						th Auflösung
						td 1280 x 720
					tr
						th Threshold
						td #{threshold}

	script(type='text/javascript').
		var detectionState = #{detectionState};
		function toggleDetection() {
			var detectionString = ''
			if(detectionState) {
				document.getElementById('detectionOn').className = 'btn btn-success';
				document.getElementById('detectionOff').className = 'btn btn-danger active';
				detectionString = 'pause';
			}else{
				document.getElementById('detectionOn').className = 'btn btn-success active';
				document.getElementById('detectionOff').className = 'btn btn-danger';
				detectionString = 'start';
			}
			detectionState = !detectionState
			var xmlhttp;
			if (window.XMLHttpRequest){
				// code for IE7+, Firefox, Chrome, Opera, Safari
				xmlhttp=new XMLHttpRequest();
			}
			else{
				// code for IE6, IE5
				xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
			}
			xmlhttp.open("GET","http://#{serverIp}:8080/0/detection/"+detectionString, true);
			xmlhttp.send();
		}